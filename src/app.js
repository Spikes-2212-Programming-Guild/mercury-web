import React from 'react'

import Routes from './routes/routes'
import 'bootstrap/dist/css/bootstrap.css'

/**
 * Calls the routing configuration to direct the user into the right page.
 */
export default () => <Routes />
